# @package _global_
# Importance Ablation: Variant 1 - Full ATAT (Learned + Frequency Prior)
# Expected PPL: 39.03 (baseline)

defaults:
  - importance_estimator_base
  - _self_

# Override key parameters for this variant
model:
  importance_mode: "full"  # Hybrid: 0.7 * learned + 0.3 * frequency_prior

trainer:
  max_steps: 5000  # DEBUG: 5K steps
