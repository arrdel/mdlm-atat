# @package _global_
# Phase 1A: Variant 1 - Full ATAT (Learned + Frequency Prior)
# Expected PPL: 39.03 (baseline)

defaults:
  - phase1a_base

# Override key parameters for this variant
model:
  importance_mode: "full"  # Hybrid: 0.7 * learned + 0.3 * frequency_prior

trainer:
  max_steps: 500000

# Logging configuration
wandb:
  project: "mdlm-atat-phase1a"
  run_name: "phase1a-full-atat"
  tags: ["phase1a", "ablation", "full"]
  notes: "Full ATAT with learned importance + frequency prior (0.7/0.3)"
