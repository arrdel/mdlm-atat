# Ablation Study: Importance Only
# Only use importance estimation, no adaptive masking or curriculum

defaults:
  - /model/tiny
  - /atat/atat_config
  - _self_

# Enable only importance estimation
use_importance: true
use_adaptive_masking: false
use_curriculum: false

training:
  importance_loss_weight: 0.1
