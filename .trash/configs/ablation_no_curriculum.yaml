# Ablation Study: No Curriculum
# Use importance and adaptive masking but no curriculum learning

defaults:
  - /model/tiny
  - /atat/atat_config
  - _self_

# Disable curriculum
use_importance: true
use_adaptive_masking: true
use_curriculum: false

training:
  importance_loss_weight: 0.1
